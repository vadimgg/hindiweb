---
/**
 * WordCard section: Usage Notes.
 *
 * Responsible for: rendering the usage notes expandable section, open by default.
 *
 * Dependencies: highlight.ts (text highlighting utility).
 */
// Responsible for: Usage Notes expandable section inside a word card, open by default
import { highlight } from '../../../utils/highlight';

interface Props {
  usageNotes: string;
}
const { usageNotes } = Astro.props;
---

<details open class="group/det border-l-2 border-l-transparent hover:border-l-amber-400/20 hover:bg-white/[0.018] transition-colors">
  <summary class="flex items-center justify-between px-4 sm:px-6 py-3 sm:py-4 cursor-pointer list-none select-none transition-colors">
    <span class="font-title text-xs sm:text-sm font-bold tracking-[0.2em] uppercase text-slate-400 group-open/det:text-slate-200 group-hover/det:text-slate-400 transition-colors">Usage Notes</span>
    <span class="text-slate-600 group-open/det:rotate-180 transition-transform duration-200 text-xs">▾</span>
  </summary>
  <div class="px-4 sm:px-6 pb-4 pt-1">
    <p class="text-[15px] text-slate-300 leading-relaxed" set:html={highlight(usageNotes)} />
  </div>
</details>
